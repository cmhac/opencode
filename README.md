# Opencode configuration

This repo contains a reusable opencode configuration. It can be cloned to ~/.config/opencode or ~/.opencode to quickly set up my preferred opencode settings, agents, mcp servers, etc.
